# Supabase / remote sync environment example
# Copy to a local .env (never commit) or export in your shell / CI secrets.

FRR_SUPABASE_URL=
FRR_SUPABASE_ANON_KEY=

# Optional: set true only in environments that should attempt remote sync
FRR_SYNC_ENABLED=false
